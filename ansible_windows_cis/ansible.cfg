[defaults]
inventory = ./inventory.yml
host_key_checking = False
# Use a control persist for SSH but for WinRM it's not relevant

[privilege_escalation]
# Windows doesn't use sudo; typically not needed

[winrm]
# Optional: settings if using pywinrm extras

[ssh_connection]
# Not used for windows but harmless